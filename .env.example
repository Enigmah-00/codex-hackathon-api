# ==============================================================================
# QueueStorm Investigator Copilot - Environment Configuration
# ==============================================================================
# This solution utilizes a 100% Deterministic, High-Efficiency Algorithmic 
# Architecture. It runs entirely on local processing logic and does not rely on 
# any external LLM providers or third-party APIs.
#
# As a result, NO API keys, tokens, or external secrets are required to run this app.
# ==============================================================================

# Server Configuration (Optional overrides if your local setup needs them)
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=production